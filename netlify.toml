[build]
  command = "cd dashboard && npm install && npm run build"
  publish = "dashboard/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Netlify environment variable example (set in dashboard site settings):
#   VITE_API_BASE_URL=https://your-bot-service.example.com
